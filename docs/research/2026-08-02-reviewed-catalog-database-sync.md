# 60 款审核产品数据库同步记录

## 本次处理结果

- 已将 60 款产品的审核描述、主图、Banner、画廊、统一规格、比较字段、Performance、FAQ 和详情 SEO 内容写入 `admin.db`。
- 产品、分类、子分类、配件及其关系均未删除；同步前后产品均为 60 款，配件均为 60 款。
- 公开产品 DTO 现在直接读取数据库审核字段，不再在每次访问时临时套用校正规则。
- 后台再次修改产品规格时，会同时更新统一比较字段，避免产品比较表与详情参数发生漂移。
- 新数据库执行 seed 时只创建缺失产品，并直接写入审核版本；已有产品仍保持不覆盖。

## 可恢复快照

- 同步前旧库：`backups/catalog/2026-08-02T08-05-00-605Z-admin.db`
- 同步前清单：`backups/catalog/2026-08-02T08-05-00-605Z-catalog-snapshot.json`
- 审核后基线库：`backups/catalog/2026-08-02T08-08-32-034Z-admin.db`
- 当前完整性基线：`backups/catalog/catalog-snapshot.json`

恢复旧库前应先停止本地或线上应用并再次备份当前数据库，不能在数据库仍有写入时直接覆盖。

## 后续维护命令

```powershell
npm run db:sync-reviewed-products
npm run db:verify-catalog
```

第一条命令会先备份当前库，再原子同步 60 款审核产品，并生成审核后基线备份；第二条命令检查产品、配件、分类、子分类数量、ID 与受保护字段。

## 验证结果

- 全量自动化测试：161/161 通过。
- ESLint：0 错误；9 条既有后台图片和 Hook 警告。
- Next.js 生产构建：203/203 个页面生成成功。
- 本地产品详情抽检：HTTP 200，审核发动机参数与 FAQ 均出现在服务端 HTML 中。
