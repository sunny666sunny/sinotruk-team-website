import Link from 'next/link'
import { ArrowRight } from 'lucide-react'
import { SiteImage } from '@/components/SiteImage'
import { productCategories } from '@/data/siteConfig'
type CatalogueCategory = Pick<(typeof productCategories)[number], 'id' | 'name' | 'description' | 'image'>
export default function CategorySection({ categories = productCategories }: { categories?: CatalogueCategory[] }) { return <section className="bg-[var(--color-ink)] py-20 text-white"><div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-[var(--color-signal)]">Vehicle range</p><h2 className="mt-3 max-w-3xl text-4xl font-extrabold leading-[.95] tracking-[-.045em]">A heavy-duty answer for every operating condition.</h2></div><Link href="/products" className="inline-flex min-h-11 items-center gap-2 text-sm font-bold text-[var(--color-signal)]">View all vehicles <ArrowRight className="h-4 w-4" /></Link></div><div className="mt-10 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{categories.map((category,index)=><Link key={category.id} href={`/products/${category.id}`} className={`group relative min-h-[22rem] overflow-hidden border border-white/15 ${index===0?'md:col-span-2 lg:col-span-2':''}`}><SiteImage src={category.image} alt={`${category.name} commercial vehicles`} fill sizes="(min-width: 1024px) 66vw, 100vw" className="object-cover transition duration-700 group-hover:scale-105"/><div className="absolute inset-0 bg-[linear-gradient(180deg,transparent_25%,rgb(7_17_31_/_0.9)_100%)]"/><div className="absolute inset-x-0 bottom-0 p-6"><p className="text-xs font-bold tracking-[.16em] text-[var(--color-signal)]">0{index+1}</p><h3 className="mt-2 text-2xl font-bold">{category.name}</h3><p className="mt-2 max-w-xl text-sm leading-6 text-slate-300">{category.description}</p><span className="mt-5 inline-flex items-center gap-2 text-sm font-bold text-white">Explore range <ArrowRight className="h-4 w-4 transition-transform group-hover:translate-x-1"/></span></div></Link>)}</div></div></section> }
