import { useEffect } from 'react'
import Link from 'next/link'
import { useRouter } from 'next/router'
import { ArrowRight, ChevronRight } from 'lucide-react'
import Header from '@/components/layout/Header'
import Footer from '@/components/layout/Footer'
import ProductCard from '@/components/product/ProductCard'
import { SeoHead } from '@/components/seo/SeoHead'
import { productCategories } from '@/data/siteConfig'
import { getProductsByCategory, type Product } from '@/data/products'

type Props={category:{id:string;name:string;categoryDescription?:string;description:string;subcategories:{id:string;name:string}[]};products:Product[]}
export default function ProductCategoryPage({category,products}:Props){const router=useRouter();const tab=typeof router.query.tab==='string'?router.query.tab:'all';const visible=tab==='all'?products:products.filter(p=>p.subcategory===tab);useEffect(()=>{if(tab!=='all'&&!category.subcategories.some(s=>s.id===tab))void router.replace(`/products/${category.id}`)},[tab,category,router]);const path=`/products/${category.id}`;return <div className="flex min-h-screen flex-col"><SeoHead input={{path,pageType:'website',name:category.name,description:category.categoryDescription||category.description,breadcrumbs:[{name:'Home',path:'/'},{name:'Products',path:'/products'},{name:category.name,path}]}}/><Header/><main id="main" className="flex-grow bg-[var(--color-panel)] pt-16 lg:pt-[72px]"><section className="border-b border-[var(--color-line)] bg-[var(--color-canvas)]"><div className="mx-auto max-w-7xl px-4 py-10 sm:px-6 lg:px-8"><div className="flex items-center gap-2 text-sm text-[var(--color-steel)]"><Link href="/">Home</Link><ChevronRight className="h-4 w-4"/><Link href="/products">Products</Link><ChevronRight className="h-4 w-4"/><span>{category.name}</span></div><div className="mt-7 grid gap-7 lg:grid-cols-[minmax(0,1fr)_20rem] lg:items-end"><div><h1 className="text-4xl font-bold tracking-[-.04em] text-[var(--color-ink)]">{category.name}</h1><p className="mt-4 max-w-3xl leading-7 text-[var(--color-steel)]">{category.categoryDescription||category.description}</p></div><p className="border-l border-[var(--color-line)] pl-5 text-sm leading-6 text-[var(--color-steel)]">Use the available records to compare vehicle requirements. Confirm configuration, availability and export terms through your RFQ.</p></div></div></section><section className="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8"><div className="flex flex-wrap gap-2" aria-label="Product subcategory filters"><Link href={path} className={`min-h-11 rounded-full border px-4 py-2 text-sm font-semibold ${tab==='all'?'border-[var(--color-signal-dark)] bg-[var(--color-signal-dark)] text-white':'border-[var(--color-line)] text-[var(--color-ink)]'}`}>All ({products.length})</Link>{category.subcategories.map(s=>{const count=products.filter(p=>p.subcategory===s.id).length;return <Link key={s.id} href={`${path}?tab=${s.id}`} className={`min-h-11 rounded-full border px-4 py-2 text-sm font-semibold ${tab===s.id?'border-[var(--color-signal-dark)] bg-[var(--color-signal-dark)] text-white':'border-[var(--color-line)] text-[var(--color-ink)]'}`}>{s.name} ({count})</Link>})}</div><div className="mt-7 flex items-center justify-between border-b border-[var(--color-line)] pb-4"><p className="text-sm text-[var(--color-steel)]">{visible.length} vehicles available in this view.</p><Link href="/shortlist" className="text-sm font-semibold text-[var(--color-signal-dark)]">View shortlist</Link></div>{visible.length?<div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{visible.map(product=><ProductCard key={product.id} product={product as any}/>)}</div>:<div className="mt-6 rounded-xl border border-dashed border-[var(--color-line)] p-10 text-center"><p className="font-semibold text-[var(--color-ink)]">No vehicles are listed in this subcategory yet.</p><Link href={path} className="mt-4 inline-block text-sm font-semibold text-[var(--color-signal-dark)]">Show all {category.name}</Link></div>}</section><section className="bg-[var(--color-ink)] py-12"><div className="mx-auto flex max-w-7xl flex-col items-start justify-between gap-6 px-4 sm:px-6 md:flex-row md:items-center lg:px-8"><div><h2 className="text-2xl font-bold text-[var(--color-panel)]">Need a configuration recommendation?</h2><p className="mt-2 text-[var(--color-canvas)]">Add relevant vehicles to your shortlist, then send operating conditions and destination details.</p></div><Link href="/contact" className="inline-flex min-h-11 items-center gap-2 rounded-lg bg-[var(--color-signal)] px-5 font-semibold text-[var(--color-ink)]">Prepare an RFQ <ArrowRight className="h-4 w-4"/></Link></div></section></main><Footer/></div>}
export async function getStaticPaths(){return{paths:productCategories.map(category=>({params:{category:category.id}})),fallback:false}}
export async function getStaticProps({params}:{params:{category:string}}){const category=productCategories.find(item=>item.id===params.category);if(!category)return{notFound:true};return{props:{category:{id:category.id,name:category.name,categoryDescription:category.categoryDescription||null,description:category.description,subcategories:category.subcategories.map(sub=>({id:sub.id,name:sub.name}))},products:getProductsByCategory(category.id)}}}
